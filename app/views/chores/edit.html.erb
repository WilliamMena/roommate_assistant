<h1>Edit <%= @chore.name %></h1>

<%= form_for @chore do |c| %>
  <%= c.label :name %>
  <%= c.text_field :name %>
  <br></br>
  Assign To:

  <%= c.collection_select :user_id, @chore.list.assign, :id, :name  %>
  <br></br>
  <%= c.submit %>
<% end %>